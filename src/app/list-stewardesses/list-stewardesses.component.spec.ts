import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListStewardessesComponent } from './list-stewardesses.component';

describe('ListStewardessesComponent', () => {
  // let component: ListStewardessesComponent;
  // let fixture: ComponentFixture<ListStewardessesComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ ListStewardessesComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(ListStewardessesComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
